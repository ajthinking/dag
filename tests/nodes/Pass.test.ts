import { Item } from '../../src/Item';
// import { Pass } from '../../src/nodes/Pass';

describe('run', () => {
  it('passes through features', async () => {
    // const item = new Item(null);
    // const node = await new Pass()
    //   .setItemsAtPortNamed('input', [item])
    //   .run();
    // expect(node.itemsAtPortNamed('output')).toEqual([item]);
  });
});
