import { Link } from '../src/Link';
import { Item } from '../src/Item';

describe('constructor', () => {
  it('sets name', async () => {
    // const node1 = new Create().portNamed('output');
    // const node2 = new Pass().portNamed('input');
    // const link = new Link({
    //   from: node1,
    //   to: node2,
    // });
    // expect(link.name).toEqual(
    //   'Create.output--->Pass.input',
    // );
  });
});
